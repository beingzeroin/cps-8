#include <stack>
#inclue<BST.h>
using namespace std;

void iterativePreorder(BSTNode *r)
{
    stack<BSTNode*> mystack;
    stack *s=createstack;

    while(1)
    {
       mystack.push(root);


    }
}
